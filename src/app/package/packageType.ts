export interface PackageType {
    companyId: Number,
    extraHrRate: Number,
    extraKmRate: Number,
    kmPerRate: Number,
    packageFor: Number,
    packageId: Number,
    packageSpecific: Number,
    packageTimeing: string,
    packageTitle: string,
    packageType: Number,
    rate: Number,
    vehicleType: Number
}